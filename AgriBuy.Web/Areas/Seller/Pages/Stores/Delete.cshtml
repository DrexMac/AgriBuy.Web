@page
@model AgriBuy.Web.Areas.Seller.Pages.Stores.DeleteModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Delete Store";
}

<div class="container mt-5">
    <h1 class="display-4 text-center">Are you sure you want to permanently delete this Store?</h1>

    @if (Model.Item == null)
    {
        <h2>No record found</h2>
    }
    else
    {
        <form method="post">
            <input type="hidden" asp-for="Item.Id" />

            <div class="mb-3">
                <label class="form-label">Store Name</label>
                <input type="text" class="form-control" asp-for="Item.Name" disabled />
            </div>

            <div class="mb-3">
                <label class="form-label">Owner/User Id</label>
                <input type="text" class="form-control" asp-for="Item.UserId" disabled />
            </div>

            <div class="form-group">
                <a asp-page="Index" class="btn btn-success">Back</a>
                <button type="submit" class="btn btn-danger">Hard Delete</button>
            </div>
        </form>
    }
</div>